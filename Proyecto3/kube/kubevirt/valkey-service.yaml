apiVersion: v1
kind: Service
metadata:
  name: valkey-service
spec:
  type: ClusterIP
  selector:
    app: valkey
  ports:
    - name: valkey
      protocol: TCP
      port: 6379
      targetPort: valkey
